spring:
  application:
    name: "customers"
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: ''
  jpa:
    hibernate:
      ddl-auto: update
  config:
    import: "configserver:http://localhost:8070/"
  h2:
    console:
      enabled: true
  cloud:
    stream:
      kafka:
        binder:
          autoCreateTopics: true
          brokers: localhost:9092
      bindings:
        customerRegistered-out-0:
          destination: customer-registered

keycloak:
  url: http://localhost:7080
  realm: transakt
  client-id: user-manager
  client-secret: Lnmq0dqeEqFXGq20MUkdvlgcEtxFsglK # NOTE: Without a persistent keycloak DB, this needs to be regenerated every time the keycloak container restarts.

server:
  port: 8080

logging:
  level:
    com:
      cdcrane:
        customers: info